(function registerIlBronzaDatatablesSummary() {
    if ($.fn.dataTable && $.fn.dataTable.Api) {
        $.fn.dataTable.Api.register('hasSummary()', function (table = null)
        {
            return this.getDataAttribute('summary', table);
        });

        $.fn.dataTable.Api.register('hasSummaryActive()', function (table = null)
        {
            if(! table)
                table = this.getTableElement();

            if(! this.hasSummary(table))
                return false;

            return this.getDataAttribute('summaryActive', table);
        });

        $.fn.dataTable.Api.register('toggleSummary()', function (table = null)
        {
            if(! table)
                table = this.getTableElement();

            if(typeof table.data('summaryActive') === 'undefined')
                return table.data('summaryActive', true);

            table.data('summaryActive', ! table.data('summaryActive'));
        });

        $.fn.dataTable.Api.register('toggleShowSelected()', function (table = null)
        {
            if(! table)
                table = this.getTableElement();

            if(typeof table.data('showSelected') === 'undefined')
                return table.data('showSelected', true);

            table.data('showSelected', ! table.data('showSelected'));
        });

        window.transformDataBySummaryExistence = function (tableId, summary, json)
        {
            return json.data;
        }

        window.populateFilteredColumnValues = function (api, tableId = null)
        {
            console.log('window.populateFilteredColumnValues');

            // let filteredRows = api.rows({filter:'applied'}).data();
            let filterRowsOptions = {};

            if(window[ tableId + 'FilteredSelected'])
                filterRowsOptions = {selected: true};

            let filteredRows = api.rows(filterRowsOptions);

            $('#' + tableId).find('thead tr.columns th').each(function(columnIndex, element)
            {
                let summary = $(element).find('input').data('summary');

                if(typeof summary === 'undefined')
                    return;

                let realColumnNumber = $(element).data('column');
                let th = $('#' + tableId + ' .inlinesearchsummary').find('.summary' + realColumnNumber);

                if((summary == 'sumMinutesArray')||(summary == 'sumSecondsArray'))
                {
                    let result = 0;
                    let totalRowsFilteredCount = 0;

                    var selection = {};

                    filteredRows.every(function (rowIdx, tableLoop, rowLoop)
                    {
                        var valueArray = this.cell(rowIdx, realColumnNumber, { search:'applied' }).data();

                        if(valueArray)
                            for (const [key, value] of Object.entries(valueArray))
                                if(! isNaN(float = parseFloat(value)))
                                    result = result + float;

                        totalRowsFilteredCount = totalRowsFilteredCount + 1;
                    });

                    if(summary == 'sumSecondsArray')
                        result = result / 60;

                    let hours = Math.floor((result / 60)) + "h";
                    let minutes = Math.floor((result % 60)) + "'";

                    result = hours + ' ' + minutes;

                    $(th).html(result);
                }

                if((summary == 'average')||(summary == 'sum')||(summary == 'sumMinutes'))
                {
                    let result = 0;
                    let totalRowsFilteredCount = 0;

                    var selection = {};

                    filteredRows.every(function (rowIdx, tableLoop, rowLoop)
                    {
                        var value = this.cell(rowIdx, realColumnNumber, { search:'applied' }).data();

                        var number = value;

                        if(Array.isArray(number))
                        {
                            if (number[number.length - 1] !== 'undefined')
                            {
                                if (!isNaN(float = parseFloat(number[number.length - 1])))

                                    _result = _result + float;
                            }
                        }
                        else if (!isNaN(float = parseFloat(number)))
                            _result = _result + float;


                        totalRowsFilteredCount = totalRowsFilteredCount + 1;
                    });

                    // api.column(columnIndex, { search:'applied' } ).data().each(function(value, rowIndex) {

                    //     // console.log('rowIndex');
                    //     // console.log(rowIndex);
                    //     // console.log(value);

                    //     // if(! isNaN(float = parseFloat(value)))
                    //     //     result = result + float;

                    //     // totalRowsFilteredCount = rowIndex;
                    // });

                    if(summary == 'average')
                        result = Math.round(((result / (totalRowsFilteredCount + 1)) + Number.EPSILON) * 100) / 100

                    if(summary == 'sumMinutes')
                    {
                        let hours = Math.floor((result / 60)) + "h";
                        let minutes = Math.floor((result % 60)) + "'";

                        result = hours + ' ' + minutes;
                    }

                    $(th).html(result);
                }
                else if((summary == 'distinct'))
                {
                    let result = new Array();

                    filteredRows.every(function (rowIdx, tableLoop, rowLoop)
                    {
                        var value = this.cell(rowIdx, realColumnNumber, { search:'applied' }).data();

                        if(typeof result[value] === 'undefined')
                            result[value] = 0;

                        result[value] ++;
                    });


                    // api.column(columnIndex, { search:'applied' } ).data().each(function(value, rowIndex) {
                    //     if(typeof result[value] === 'undefined')
                    //         result[value] = 0;

                    //     result[value] ++;
                    // });

                    let string = new Array;

                    Object.keys(result).map(function(key, index)
                    {
                        string.push(key + ': ' + result[key]);
                    });

                    $(th).html(string.join('<br />'));
                }

            });

        }
    } else {
        // Riprova dopo un attimo
        setTimeout(registerIlBronzaDatatablesSummary, 100);
    }
})();